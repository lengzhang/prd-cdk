{
  "name": "prd-cdk",
  "description": "CDK for property reports dashboard",
  "version": "0.1.0",
  "scripts": {
    "dev": "NODE_ENV=dev && npm run build",
    "dev-win": "set NODE_ENV=dev&& npm run build",
    "build": "tsc && npm run test && cdk ls",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "clean": "rm -rf dist cdk.out"
  },
  "devDependencies": {
    "@types/jest": "^29.5.5",
    "@types/node": "20.7.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "aws-cdk": "2.103.1",
    "ts-node": "^10.9.1",
    "typescript": "~5.2.2"
  },
  "dependencies": {
    "aws-cdk-lib": "2.103.1",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.21"
  }
}
